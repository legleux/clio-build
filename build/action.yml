name: 'build clio'
description: 'Build clio'
runs:
  using: "composite"

  steps:
    - name: Build
      shell: bash
      run: |
        ls .
        find . -name build_clio.sh
        find ${{github.workspace}} -name build_clio.sh
        ./build_clio.sh ${{matrix.type.suffix}}
